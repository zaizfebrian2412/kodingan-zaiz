# ============================================================
# Dependency untuk prototype Klasifikasi Citra Sampah (CNN)
# ============================================================
# Catatan versi:
# - tensorflow 2.13-2.16 kompatibel dengan format model ".keras"
#   dan layer preprocessing (RandomFlip, RandomRotation, dll).
# - numpy < 2.0 wajib agar cocok dengan TensorFlow pada rentang ini.
# - Disarankan menggunakan Python 3.10 / 3.11 (lihat README).
#
# (Opsional, hanya untuk Mac Apple Silicon dengan akselerasi GPU Metal:
#   pip install tensorflow-metal )
# ============================================================

flask>=2.3,<4.0
tensorflow>=2.13,<2.17
numpy<2.0
pillow>=9.5
opencv-python>=4.7
scikit-learn>=1.3
matplotlib>=3.7
